The object detection Django app is used to mark objects on images from trained data available in form of annotations in xml file.

The home page consist of fields to upload image file and XML file. On submission , the object marked image on browser.

On report page, the user can select start and end date to generate csv file of database

The database here used is mysql as it is easy to integrate with Django and is suitable for current application